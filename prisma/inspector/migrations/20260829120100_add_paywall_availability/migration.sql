-- AlterEnum
-- PAYWALL: the copy exists online but sits behind a subscription/paid access
-- (e.g. LIBRARIA), as opposed to RESTRICTED (gated for other reasons).
ALTER TYPE "Availability" ADD VALUE 'PAYWALL';
